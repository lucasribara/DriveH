#include <stdio.h>

int main()

{
    int i, j, n;

    printf("Digite a medida do quadrado: ");
    scanf("%d", &n);


  	for (j = 1; j <=n; j++)
    {

      for (i= 1; i <= n; i++)
        {
         	if ( i == 1 || i == n )
				printf ( j == 1 || j == n ? "+": "|");
			else
				printf ( j == 1 || j == n ? "-": " ");
			printf("\n");   
        }
    }


    return 0;
}
